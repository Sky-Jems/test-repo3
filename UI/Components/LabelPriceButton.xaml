<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="POS.Components.LabelPriceButton"
            x:Name="LabelPriceView"
            Padding="10">

    <Frame CornerRadius="10" 
        HorizontalOptions="Center"
        VerticalOptions="Center"
        Padding="16"
        BackgroundColor="{Binding ButtonColor, Source={x:Reference LabelPriceView}}"
        HeightRequest="{Binding ButtonHeight, Source={x:Reference LabelPriceView}}"
        WidthRequest="{Binding ButtonWidth, Source={x:Reference LabelPriceView}}">

        <StackLayout>
            <!-- Label -->
            <Label Text="{Binding Label, Source={x:Reference LabelPriceView}}"
                FontSize="Large"
                TextColor="Black"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                HeightRequest="46.5"/>

            <!-- Price -->
            <Label Text="{Binding Price, Source={x:Reference LabelPriceView}}"
                FontSize="Large" 
                TextColor="Black"
                HorizontalTextAlignment="Center"
                VerticalTextAlignment="Center"
                HeightRequest="46.5"/>
            </StackLayout>
        </Frame>
</ContentView>
